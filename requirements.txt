# --- Núcleo del pipeline ---
pydantic>=2.6
python-dotenv>=1.0

# --- TTS (Piper, licencia MIT, corre en CPU) ---
piper-tts>=1.2.0

# --- Imágenes ---
Pillow>=10.2

# --- API + interfaz gráfica ---
fastapi>=0.110
uvicorn[standard]>=0.27
# necesario para que la interfaz pueda subir imágenes/audio (multipart/form-data)
python-multipart>=0.0.9

# --- Utilidades ---
ffmpeg-python>=0.2.0
